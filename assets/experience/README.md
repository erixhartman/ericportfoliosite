# Experience images

Replace any SVG in this folder with a real product screenshot using the same filename, or update the `image` path in `content.js`.

Recommended export:
- 1600 x 1200 px (4:3)
- JPG, PNG, WebP, or SVG
- Crop out private customer data, internal dashboards, unreleased features, and confidential metrics
- Keep the focal area near the centre because the layout uses `object-fit: cover`

Example:
```js
image: "assets/experience/sportsnet-plus.webp",
imageAlt: "Sportsnet+ live game playback screen",
```
